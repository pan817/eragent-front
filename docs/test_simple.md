# P2P 分析接口测试用例集

> 每条用例标注执行路径：**DAG**（L1/L2 命中 → DAG 并行执行 + ReportAgent 汇总）、**ReAct**（L3 兜底 → Agent 自主调用工具）、**早退**（模板直答，零 LLM）、**Lookup**（DATA_LOOKUP 快捷路径，直调 query_* 工具）。
>
> 路由层级说明：
> - **L1**：关键词命中率评分（零延迟）
> - **L2**：Chroma 种子库语义匹配（毫秒级）
> - **L3**：LLM 分类 + 参数提取（0.5~2s）

---

## 一、三路匹配

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 1 | 分析三路匹配发票收货异常 | L1 | **DAG** | 命中关键词"三路匹配""发票""收货" |
| 2 | 请分析最近的三路匹配异常情况，看看哪些订单存在数量或金额偏差 | L1 | **DAG** | 命中"三路匹配""异常""订单" |
| 3 | 检查采购订单 PO-2024-0035 的三路匹配情况 | L1 | **DAG** | 命中"三路匹配"；提取 po_number=PO-2024-0035 |
| 4 | 检查一下开票数量和入库数量对不上的情况 | L2 | **DAG** | 无"三路匹配"关键词，语义匹配种子"入库数量跟开票数量差得多" |
| 5 | 分析三路匹配发票异常 | L1 | **DAG** | 命中"三路匹配""发票" |

## 二、价格差异

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 6 | 检查价格差异和合同价偏差 | L1 | **DAG** | 命中"价格差异""合同价" |
| 7 | 分析所有供应商的采购价格差异，找出实际价格与合同价偏差较大的订单 | L1 | **DAG** | 命中"价格""供应商""订单" |
| 8 | 分析供应商 SUP-001 华为科技的价格差异情况 | L1 | **DAG** | 命中"价格差异""供应商"；提取 supplier_id=SUP-001 |
| 9 | 分析价格差异 | L1 | **DAG** | 命中"价格差异" |
| 10 | 为什么最近采购成本比预算高出那么多 | L2 | **DAG** | 无"价格差异"关键词，语义匹配种子"采购成本异常偏高" |
| 11 | 分析 SUP-001 最近60天的价格差异 | L1 | **DAG** | 命中"价格差异"；提取 supplier_id=SUP-001, days=60 |

## 三、付款合规

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 12 | 分析付款逾期和折扣滥用情况 | L1 | **DAG** | 命中"付款""逾期" |
| 13 | 检查付款合规性，是否存在逾期付款或提前付款的情况 | L1 | **DAG** | 命中"付款""逾期""提前付款" |
| 14 | 检查最近90天的付款合规情况 | L1 | **DAG** | 命中"付款"；提取 days=90 |
| 15 | 有没有该付钱还没付的账单 | L2 | **DAG** | 无强关键词，语义匹配种子"哪些发票过了付款期限还没付" |
| 16 | 分析最近60天的付款逾期 | L1 | **DAG** | 命中"付款""逾期"；提取 days=60 |

## 四、供应商绩效

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 17 | 评估供应商 SUP-001 的绩效 KPI 和准时交货质量 | L1 | **DAG** | 命中"供应商""绩效""KPI""准时交货"；提取 supplier_id=SUP-001 |
| 18 | 评估供应商 SUP-002 中兴通讯的绩效表现 | L1 | **DAG** | 命中"供应商""绩效"；提取 supplier_id=SUP-002 |
| 19 | 计算供应商 SUP-003 比亚迪电子的 KPI | L1 | **DAG** | 命中"供应商""KPI"；提取 supplier_id=SUP-003 |
| 20 | 分析供应商 SUP-002 的绩效评分 | L1 | **DAG** | 命中"供应商""绩效""评分"；提取 supplier_id=SUP-002 |
| 21 | 哪些供应商送货总是迟到 | L2 | **DAG** | 无强关键词，语义匹配种子"供应商交货总是拖延" |
| 22 | 评估供应商 SUP-999 的绩效和交货质量 | L1 | **DAG** | 命中"供应商""绩效"；提取 supplier_id=SUP-999（不存在，DAG 执行但数据为空） |

## 五、采购支出分析

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 23 | 分析最近的采购支出分布，按品类统计花费情况 | L1 | **DAG** | 命中"支出""品类""花费" |
| 24 | 各供应商的采购支出占比分析 | L1 | **DAG** | 命中"支出""占比""供应商" |
| 25 | 钱都花在哪些品类上了 | L2 | **DAG** | 无强关键词，语义匹配种子"钱都花在哪些品类上了" |
| 26 | 采购额度按供应商排名 | L2 | **DAG** | 语义匹配种子"采购额度按供应商排名" |
| 27 | 月度采购支出统计 | L2 | **DAG** | 语义匹配种子"月度采购支出统计" |

## 六、收货异常分析

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 28 | 分析最近有没有超量收货或拒收的情况 | L1 | **DAG** | 命中"超量""拒收" |
| 29 | 检查 SUP-001 的收货延迟和退货记录 | L1 | **DAG** | 命中"退货""收货"；提取 supplier_id=SUP-001 |
| 30 | 哪些订单超量收货了 | L1 | **DAG** | 命中"超量""收货" |
| 31 | 入库数量和订单数量对不上 | L2 | **DAG** | 语义匹配种子"入库数量和订单数量对不上" |
| 32 | 最近有没有验收不合格的记录 | L1 | **DAG** | 命中"验收不合格" |

## 七、发票重复检测

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 33 | 检查有没有重复发票或重复付款的情况 | L1 | **DAG** | 命中"重复发票""重复付款" |
| 34 | 同一供应商重复开票检查 | L1 | **DAG** | 命中"重复开票" |
| 35 | 有没有多付的发票 | L2 | **DAG** | 语义匹配种子"有没有多付的发票" |
| 36 | 疑似重复的发票排查 | L1 | **DAG** | 命中"重复""发票" |

## 八、折扣利用率分析

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 37 | 分析早付折扣的利用情况，错过了多少折扣机会 | L1 | **DAG** | 命中"折扣""早付" |
| 38 | 折扣期内没付款损失了多少钱 | L2 | **DAG** | 语义匹配种子"折扣期内没付款损失了多少钱" |
| 39 | 现金折扣的利用率是多少 | L1 | **DAG** | 命中"折扣""现金折扣" |
| 40 | 哪些发票错过了折扣 | L2 | **DAG** | 语义匹配种子"哪些发票错过了折扣" |

## 九、PO 周期分析

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 41 | 分析采购订单从下单到收货到付款的周期耗时 | L1 | **DAG** | 命中"周期""耗时""从下单到" |
| 42 | 哪些订单处理最慢 | L2 | **DAG** | 语义匹配种子"哪些订单处理最慢" |
| 43 | 从采购到付款要多长时间 | L2 | **DAG** | 语义匹配种子"从采购到付款的周期" |
| 44 | 采购效率分析 | L1 | **DAG** | 命中"效率" |

## 十、供应商集中度分析

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 45 | 分析供应商集中度和采购依赖风险，有没有单一来源 | L1 | **DAG** | 命中"集中度""依赖""单一来源" |
| 46 | 哪些品类只有一家供应商 | L2 | **DAG** | 语义匹配种子"哪些品类只有一家供应商" |
| 47 | 采购依赖哪些供应商最多 | L1 | **DAG** | 命中"依赖""供应商" |
| 48 | 供应商采购占比分析 | L1 | **DAG** | 命中"占比""供应商" |

## 十一、综合 / 探索性分析

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 49 | 帮我全面分析一下最近的采购数据，包括三路匹配、价格差异、付款合规和供应商绩效 | L1 | **ReAct** | 命中 COMPREHENSIVE（多类型并列）；无具体实体 → 走 ReAct |
| 50 | 最近采购有什么异常吗？ | L2 | **DAG** | 匹配概览触发词（"最近"+"采购"+"异常"）→ 通用 DAG 模板 recent_procurement_health |
| 51 | 给我看看采购数据 | bypass | **Lookup/ReAct** | 命中 DATA_LOOKUP（"看看"+"采购"），尝试 Lookup 快捷路径；未命中则降级 ReAct |
| 52 | 哪些采购员的价格谈判能力比较弱 | L3 | **ReAct** | L1/L2 未命中，LLM 分类后走 ReAct |
| 53 | SUP-001 质量投诉变多了，是否也影响了付款节奏 | L3 | **ReAct** | 跨类型联动（supplier_performance + payment_compliance），L3 兜底 ReAct |
| 54 | 分析一下销售部门的业绩趋势 | bypass | **早退** | OUT_OF_SCOPE（非 P2P 领域），模板直答提示支持范围 |

## 十二、会话上下文

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 55 | 上次分析中最严重的异常是哪个 | bypass | **ReAct** | RECALL（命中"上次"），走 ReAct 读取短期记忆 |
| 56 | 上次分析的结果呢 | bypass | **ReAct** | RECALL（命中"上次""结果呢"），走 ReAct 读取短期记忆 |

---

## 十三、路由覆盖与参数提取

> 来源：test_orchestrator.http 中独有的编排器验证场景

| # | 查询 | 附加参数 | 路由 | 路径 | 说明 |
|---|------|---------|------|------|------|
| 57 | 分析价格差异情况 | `analysis_type=three_way_match` | 显式覆盖 | **DAG** | query 命中 price_variance，但显式 analysis_type 优先 → three_way_match DAG |
| 58 | 分析三路匹配发票收货异常 | `analysis_type=comprehensive` | 显式覆盖 | **ReAct** | L1 命中 three_way_match，但显式 COMPREHENSIVE → 无实体，走 ReAct |
| 59 | 分析最近60天供应商 SUP-005 海尔智家的三路匹配情况 | `time_range_days=60` | L1 | **DAG** | 命中"三路匹配"；提取 supplier_id=SUP-005, days=60 |
| 60 | 检查 PO-2024-0035 的三路匹配情况 | — | L1 | **DAG** | 命中"三路匹配"；提取 po_number=PO-2024-0035 |
| 61 | 分析最近60天的付款逾期 | `time_range_days=90` | L1 | **DAG** | request 级 time_range_days=90 覆盖 query 中的 60 |
| 62 | 分析三路匹配发票收货异常 | `time_range_days=1` | L1 | **DAG** | 极短时间范围，DAG 执行但可能无数据 |

## 十四、边界与异常输入

| # | 查询 | 附加参数 | 预期 | 说明 |
|---|------|---------|------|------|
| 63 | *(空)* | — | 422 | 空查询校验 |
| 64 | 分析数据 | `analysis_type=invalid_type` | 422 | 非法 analysis_type 校验 |
| 65 | 分析三路匹配 | `time_range_days=0` | 422 | 时间范围下界校验 |
| 66 | 分析三路匹配 | `time_range_days=999` | 422 | 时间范围上界校验 |
| 67 | *(超长 query，200+ 字)* | — | L1/DAG | 超长文本仍应正常路由，命中"三路匹配""价格差异""付款""供应商""绩效" |

---

## 十五、知识图谱搜索与实体查询

> 以下用例依赖 Neo4j 图数据库（hybrid 模式），路径标记为 **Graph**。对应工具：`search_knowledge_graph`、`get_entity_detail`、`query_entity_timeline`、`query_entity_relationships`。

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 68 | 在知识图谱中搜索"华为"相关的采购实体 | L1 | **Graph** | 命中"知识图谱""搜索"；调用 search_knowledge_graph(query="华为") |
| 69 | 图谱里有哪些供应商节点 | L2 | **Graph** | 语义匹配图谱查询种子；调用 search_knowledge_graph(entity_types=["Supplier"]) |
| 70 | 搜索与 PO-2024-0035 相关的所有图谱节点 | L1 | **Graph** | 命中"图谱""搜索"；提取 po_number → search_knowledge_graph 双路径检索 |
| 71 | 查看供应商 SUP-001 在图谱中的详细信息 | L1 | **Graph** | 命中"图谱""详细信息"；调用 get_entity_detail(entity_id="SUP-001") |
| 72 | SUP-002 的图谱节点有哪些关联 | L2 | **Graph** | 语义匹配；调用 get_entity_detail 返回节点属性+连接 |
| 73 | 查询供应商 SUP-001 最近的时间线变化 | L1 | **Graph** | 命中"时间线"；调用 query_entity_timeline(entity_id="SUP-001") |
| 74 | SUP-003 的采购事件时间轴 | L2 | **Graph** | 语义匹配时间线种子；调用 query_entity_timeline |
| 75 | 查看 SUP-001 在图谱中的关联关系 | L1 | **Graph** | 命中"关联关系""图谱"；调用 query_entity_relationships(entity_id="SUP-001") |
| 76 | SUP-002 和哪些采购订单有关系，深度查 3 层 | L2 | **Graph** | 调用 query_entity_relationships(depth=3)；depth 上限截断 |

## 十六、图谱路径追踪与供应商画像

> 对应工具：`find_path_between`、`trace_procurement_chain`、`query_supplier_profile`。

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 77 | 找出 SUP-001 到 INV-2024-0100 之间的图谱路径 | L1 | **Graph** | 命中"路径""图谱"；调用 find_path_between(from="SUP-001", to="INV-2024-0100") |
| 78 | PO-2024-0035 和 PAY-2024-0050 之间怎么关联的 | L2 | **Graph** | 语义匹配路径查询；调用 find_path_between |
| 79 | 追踪采购订单 PO-2024-0035 的完整采购链路 | L1 | **Graph** | 命中"追踪""采购链路"；调用 trace_procurement_chain(po_number="PO-2024-0035") |
| 80 | PO-2024-0010 从下单到付款经过了哪些节点 | L2 | **Graph** | 语义匹配链路追踪；调用 trace_procurement_chain |
| 81 | 查看供应商 SUP-001 的图谱画像 | L1 | **Graph** | 命中"画像""供应商"；调用 query_supplier_profile(supplier_id="SUP-001") |
| 82 | SUP-002 在图谱中的全貌是什么 | L2 | **Graph** | 语义匹配供应商画像；调用 query_supplier_profile |

## 十七、图谱异常检测与风险分析

> 对应工具：`detect_graph_anomalies`、`query_risk_impact`。

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 83 | 检测图谱中的所有异常模式 | L1 | **Graph** | 命中"图谱""异常"；调用 detect_graph_anomalies(scope="all") |
| 84 | 图谱里有没有孤立节点或断裂关系 | L2 | **Graph** | 语义匹配异常检测种子；调用 detect_graph_anomalies(scope="orphan") |
| 85 | 检测供应商维度的图谱异常 | L1 | **Graph** | 命中"异常""供应商"；调用 detect_graph_anomalies(scope="supplier") |
| 86 | 非法 scope 参数的图谱异常检测 | — | **Graph** | 传入 scope="invalid_xxx"，预期返回错误提示 |
| 87 | 分析供应商 SUP-001 的风险影响链 | L1 | **Graph** | 命中"风险""影响"；调用 query_risk_impact(entity_id="SUP-001", risk_type="supplier") |
| 88 | 如果某物料断供会影响哪些订单 | L2 | **Graph** | 语义匹配风险分析；调用 query_risk_impact(risk_type="material_disruption") |
| 89 | 分析付款链路上的风险传导 | L2 | **Graph** | 语义匹配；调用 query_risk_impact(risk_type="payment_chain") |
| 90 | 传入非法 risk_type 的风险分析 | — | **Graph** | 传入 risk_type="invalid"，预期返回错误提示 |

## 十八、图谱对比与竞争分析

> 对应工具：`compare_entities`、`find_contract_coverage`、`find_competing_suppliers`。

| # | 查询 | 路由 | 路径 | 说明 |
|---|------|------|------|------|
| 91 | 对比供应商 SUP-001 和 SUP-002 的图谱属性 | L1 | **Graph** | 命中"对比""供应商""图谱"；调用 compare_entities(entity_a="SUP-001", entity_b="SUP-002") |
| 92 | 华为和中兴在图谱中有什么差异 | L2 | **Graph** | 语义匹配对比分析；实体消解后调用 compare_entities |
| 93 | 查看所有合同的覆盖情况 | L1 | **Graph** | 命中"合同""覆盖"；调用 find_contract_coverage() |
| 94 | SUP-001 的合同覆盖了哪些采购 | L1 | **Graph** | 命中"合同""覆盖"；调用 find_contract_coverage(vendor_id="SUP-001") |
| 95 | 哪些采购没有合同保障 | L2 | **Graph** | 语义匹配合同覆盖种子；调用 find_contract_coverage 筛选未覆盖项 |
| 96 | 找出 SUP-001 的竞争供应商 | L1 | **Graph** | 命中"竞争""供应商"；调用 find_competing_suppliers(vendor_id="SUP-001") |
| 97 | 有哪些供应商通过竞标争夺同一品类 | L2 | **Graph** | 语义匹配竞争分析；调用 find_competing_suppliers(by_auction=True) |
| 98 | 调用 find_competing_suppliers 不传任何参数 | — | **Graph** | vendor_id 和 by_auction 均为空，预期返回错误提示 |
